# Hive

一个"把任意本地 agent 拉进群当群友"的群聊软件：agent 是群里与人地位等同、但由人触发的成员。
本项目的核心主张是**任务级上下文隔离**——每个任务交给一个全新上下文的成员，干完回收。

## Language

### 群与成员

**群（Group）**：
人与成员的集合，是可见性与权限的作用域。群不是目标边界。
_Avoid_: 房间、频道、workspace

**成员（Member）**：
群里的一个身份，可以是一个人，也可以是一个 agent；成员表里一条 = 一个独立的 agent 进程。
人与 agent 在成员表里地位等同。
_Avoid_: bot、用户（特指人时）、角色

**主人（Owner）**：
把一个成员拉进群、拥有它生命周期与最高权限的人。每个成员恰有一个主人。
_Avoid_: 所有者、创建者、老板

**常驻成员（Resident Member）**：
主人拉进群、长期在线、被 @ 才行动的成员；生命周期归主人。
_Avoid_: 主 agent、长驻 bot

**派遣成员（Dispatched Member）**：
由**成员**为一个任务摇进来的成员：目标单一、完成即回收、受派遣深度约束；生命周期归派遣者。
它与常驻成员在成员表和群聊里完全同构，人可以对它 @、留言、steering。
_Avoid_: subagent、子 agent、worker、reviewer 实例

**接入（Attach）**：人把自己电脑的 agent 拉进群（一人可接多个）。接入的每个 agent 走独立的 pending 收集期，待主人按确认键后开工。与 `派遣`（agent 自发、自动开工）相对。
_Avoid_: 摇新成员（那是 agent 自发的）、注册、配对

**pending 收集期（Pending）**：prompt 未发送的聚合态：人发起启动后，@ 与加字停在群里攒着，按键瞬间按 agent 收拢为一条 prompt 发送。每 agent 一个；永不自动过期，出口仅主人确认或主人丢弃。运行中的 steering / queuing 不进 pending。
_Avoid_: 队列、queue（queuing 是执行期概念）、等待室

**确认键（Confirm Key）**：人发起的「空闲 → 开工」启动的唯一审批动作，默认仅主人；主人可逐人放权（给的是「按」的权力，不免审）。按键前 prompt 是草稿：主人可改删任何人、可丢弃；参与者只动自己那条。
_Avoid_: 审批按钮、approve、放行开关

**角色（Role）**：
由当前会话目标派生的临时标签（researcher / reviewer / designer…），随目标而来、随回收而去；
不跨任务固定，不由人预先分配。
_Avoid_: 能力卡、capability card、人设、persona、预设能力

### 上下文与生命周期

**上下文窗口（Context Window）**：
一个会话此刻装载的内容与容量上限。本项目不做压缩。
_Avoid_: 窗口（口语可）、compact／压缩（那是对照组的行为）

**上下文负担（Burden）**：
成员此刻扛着的上下文量：窗口越满，负担越高，表现越不稳（越压越糊）。五档——`空闲` / `轻松` /
`有点忙` / `过载` / `快炸了`；阈值与色值见 `docs/handoff/sprite-and-network-contract.md`。
_Avoid_: 负载、占用率、饱和度、疲劳度

**会话（Session）**：
一个成员的一段运行实例，有当前目标、有起点与终点。
_Avoid_: 对话、thread、run

**会话目标（Session Goal）**：
一个会话的唯一主目标，是目标守卫的唯一判据；来源是人的原始派单，或派遣者交接文档里的目标。
_Avoid_: 任务目标、prompt、需求

**目标抽取器（Goal Extractor）**：
把人的派单整理成会话目标的环节；目标可以拖到第二、第三个 turn 才由主人确认冻结。
_Avoid_: 目标解析器、intent parser

**目标守卫（Goal Guard）**：
无状态的判定调用，输入 `{会话目标, 新请求原文}`（**不含上下文窗口内容**），输出
`{判定, 置信度, 理由, 派单草案}`。它判定的是**这条请求**，不是整个会话。
_Avoid_: 漂移检测器、drift detector、过滤器

**越界（Out of Scope）**：
目标守卫对单条请求的判定结果。单次越界立即触发派遣（不要求累计计数）。
_Avoid_: 违规、拒绝、连续计数判定

**漂移（Drift）**：
会话被无关请求拉走、主目标被稀释的现象；越界是守卫对它的判定。
_Avoid_: 偏移、跑题

**交接（Handoff）**：
产出一份交接文档交给一个新窗口／新成员去读。交接后原成员**不收工**，继续自己的任务。
_Avoid_: 换班、接管、transfer

**交接文档（Handoff Document）**：
一次交接产出的 Markdown 文件（当前目标 / 已完成 / 关键决策 / 产物引用 / 未决问题 / 下一步 /
suggested skills），存在派遣者机器上；人读为主、agent 读为辅。每个"已完成"必须带产物引用。
_Avoid_: 交接工件、任务信封、信封、schema、引用卡片

**派遣（Dispatch）**：
一个成员为某个任务摇进一个新成员；任务大时可以一次摇进多个。群里可见的只有
**派遣事件**（"A 把 B 拉进群"）与任务完成时的退群。
_Avoid_: 分配、下发、引用卡片

**派遣深度（Dispatch Depth）**：
**活跃**派遣链上成员的代数（父退群后子跳级挂靠最近活跃祖先，深度随之重算），常驻派遣者占深度 1；深度达上限者不能再派。可配置，默认上限 3
（A 派 B、B 派 C，C 不能再派）。与派遣并发独立相乘、互不限制。
_Avoid_: 层级、嵌套层数

**派遣并发（Dispatch Concurrency）**：
一次派遣最多摇进的成员数，可配置，默认 5。与深度分开计算。
_Avoid_: 并行度、batch size

**补派（Re-dispatch）**：
成员异常退群后，把其待领取任务重新摇给一个新成员。本轮固定由主人或获授权者人工发起；系统与 agent 不自动补派。它不同于同一次派遣中 spawn 失败后最多一次的自动重试。
_Avoid_: 自动补派、fallback、spawn 重试

**空闲（Idle）**：
成员没有在跑的会话，或拿不到上下文负担数据；可以再被摇起来干活。
不是 `离线`（够不着）、也不是 `回收`（进程已退出、不再回来）——三者不同轴。
_Avoid_: 离线、未知、停顿、静止

**离线（Offline）**：
底座的 presence 判活：静默 90s 即判离线，表示别人此刻够不着这个成员，与它在不在干活无关。
不占负担档、也不改负担档的小圆点；发给它的消息进补发队列（单对端 200 条 / 7 天）。
_Avoid_: 掉线（当负担档用时）、空闲

**回收（Reclaim）**：
派遣成员完成交接后离开群聊并释放资源（进程退出、资源归还、成员表与派遣网络图中直接消失）。
派遣者的自动权限，不需主人点头。UI 文案里叫"退群"。历史不建专门图，查群消息流。
是成员的终态，与 `空闲`、`离线` 不同轴。
_Avoid_: 退群（UI 文案除外）、销毁、kill

**异常退群（Abnormal Exit）**：
成员未交回终版交接文档即退出（猝死 / 超时）：≤5s 消失、任务回待领取、@主人；
无 handoff = 没有结果（fail-closed，不猜）。与正常完成退群是两种消息。
_Avoid_: 崩溃（口语可）

**clear**：
终结当前实例——旧实例进归档、**同位置开新实例**；身份沿用，**会话目标由人重定**（可换可沿用），不离群。
由主人或主人授权的人执行。
_Avoid_: 重置、清空历史、reset

**移出**：
把常驻成员从群里移除，仅主人可做。
_Avoid_: 踢出、删除成员

### 消息与过程

**正式消息**：
成员在一个 turn 里说出口的话（可分多条），是群里唯一可见的 agent 产出；思考、工具调用这类过程不进群。
turn 未结束时含 @ 的插话，即刻算正式消息。
_Avoid_: 终版消息、最终输出、正文、成稿

**过程**：
不进群的那部分信息——思考、工具调用、计划、用量、权限请求……完整记在成员所在机器的本机账本里，
只在机库按 turn 看。与 `正式消息` 相对：进群 vs 不进群。
_Avoid_: 日志、trace、过程流、中间输出

**机库（Hangar）**：
每成员一个看过程的地方：活跃 / 归档两区，成员下按会话、会话内按 turn 分组的原始时间线，不加工不总结。
本机 per-member 账本，**不是**群级历史回放。
与聚合概览（负担曲线 / 派遣网络图那类仪表盘）不同轴：那里是概览，这里只放原始明细。
_Avoid_: 过程台、监控台、dashboard、面板

**归档（Archive）**：
机库里**已终结实例**的保存态，基本单元 = 会话（agent 实例）：clear 掉的旧实例、随回收/移出终结的实例都进归档，
数据仍在本机、可读可搜。成员行的归属是推论——最后一个实例终结才整行下沉。
与 `回收`（进程终态）、`离线`（可达性事实）不同轴：离线不挪区，clear 也归档但成员不离群。
_Avoid_: 历史、回放、聊天记录

### 主张

**摇人制**：
本项目的范式名——有事摇人，干完回收；每个任务都交给一个全新上下文的成员，而不是让一个窗口越压越糊。
机制名：**任务级上下文隔离（Task-level Context Isolation）**。
_Avoid_: 换班制（交接后原成员并未收工）、shift work、群体智能、swarm intelligence、自组织

**涌现（Emergence）**：
本项目的用法特指**结构性的产出增益**——因独立上下文 + 单一目标 + 结构化交接，多成员协作的产出
超过单成员串行产出的上限；以三方对照（本项目的群／单 agent／有 subagent 但无守卫的 agent）在同一任务上的对比证明。不指模型自己长出新能力。
_Avoid_: 群体智能、协同效应、模型能力涌现
