HIVE / LIQUID NEURAL SPACE — FRONTEND PACKAGE

Production build: dist/
Deploy the contents of dist/ to a static web host, or serve dist/ with any static HTTP server. Open index.html through HTTP/HTTPS; WebGL assets may not work when opened as file://.

Source code: src/
Public assets: public/

Development:
  pnpm install
  pnpm dev

Production build:
  pnpm build

This archive excludes node_modules, local screenshots, and work-only sound candidates. The built dist/ already contains all runtime assets.
